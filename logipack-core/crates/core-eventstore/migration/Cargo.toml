[package]
name = "core-eventstore-migration"
version = "0.1.0"
edition = "2024"
publish = false

[lib]
name = "core_eventstore_migration"
path = "src/lib.rs"

[dependencies]
sea-orm-migration = { version = "1", features = ["runtime-tokio-rustls", "sqlx-postgres"] }
